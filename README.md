# farmbiddy-forecast-prototype

## Run locally (Windows)

```powershell
python -m pip install -r requirements.txt
.\start.bat
```

Or: `python -m uvicorn api.main:app --reload` then open http://127.0.0.1:8000